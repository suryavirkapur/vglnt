# vglnt-data

## What does this do?

Prepares the data for the vglnt-app. Downloads the data from the internet and stores it in the `../../data` folder, and then runs the `prepare.py` script to prepare the data, split the video into frames, and create the `.json` files.
